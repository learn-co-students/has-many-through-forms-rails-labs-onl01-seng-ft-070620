<!-- <h3> Title: <%= @post.title %></h3>

<p> <% @post.categories.each do |category| %> 
        <%= category.name %> | 
    <% end %>
</p>

<p> Post: <%= @post.content %> </p>


<h4> Comments:</h4>

<%= form_for @post.comments.build do |f| %>
    <%= f.hidden_field :post_id %>

    <%= f.label :content, "New Comment:" %>
    <%= f.text_field :content %>  <br><br>
    
    <%= f.label "Select your username:" %>
    <%= f.collection_select :user_id, User.all, :id, :username %> <br><br>

    <%= f.label "Or create a new one:" %><br><br>
    <%= f.fields_for :user, User.new  do |user_fields| %>

        <%= user_fields.label :username %> 
        <%= user_fields.text_field :username %> <br> <br>

        <%= user_fields.label :email %> 
        <%= user_fields.text_field :email %> <br> <br>

    <% end %>

    <%= f.submit %>
<% end %>

<ul>
    <% @post.comments.each do |comment| %> 
        <li> <%= comment.user %> says: <%= comment.content %> </li>
    <% end %>
</ul> -->